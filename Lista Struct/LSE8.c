/*Crie um programa que converta coordenadas polares para cartesianas, isto  é:
(a) Crie uma struct que represente um ponto em coordenadas polares: um raio (r), e
um argumento (ângulo theta), dado em radianos.

(b) Crie uma struct que represente um ponto em coordenadas cartesianas: duas coor-
denadas reais, x e y.

(c) No programa principal, leia um ponto em coordenada polar e converta-o para co-
ordenadas cartesianas através da fórmula: x = r cos(theta) e y = r sen(theta).

Grave o resultado em uma variável do tipo coordenada cartesiana.*/

#include <stdio.h>

int main(void) {
  printf("Hello World\n"); //Parabéns Lucão
  return 0;
}